machine:
  node:
    version: 0.10.22

dependencies:
  override:
  - npm install
  - bower install

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync dist/ s3://circles3bucket/ --delete
