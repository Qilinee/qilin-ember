machine:
  node:
    version: 0.10.22

dependencies:
  override:
  - npm install
  - bower install
  - npm install -g ember-cli

deployment:
  prod:
    branch: master
    commands:
      - ember build
      - aws s3 sync dist/ s3://circles3bucket/ --delete
